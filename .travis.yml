language: bash
sudo: required
services:
  - docker


# Before
before_install:
  - docker version
  - sudo docker run hello-world
  - sudo docker-compose up
  - sudo docker images
#install:
  #- sudo docker-compose up -d nginx mysql workspace

  

